import SignupClient from "./SignupClient";

export const metadata = {
  title: "Criar Conta | APEX COACH",
  description: "Criar conta de coach para entrar na APEX COACH no browser.",
};

export default function SignupPage() {
  return <SignupClient />;
}
